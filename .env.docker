# Environment Configuration
NODE_ENV=production
PORT=3000

# Database Configuration (SQLite for Docker)
DATABASE_URL=file:./fleet_management.db

# JWT Configuration
JWT_SECRET=docker-fleet-management-jwt-secret-key-2024
JWT_EXPIRES_IN=7d

# Application Configuration
API_PREFIX=api